import 'package:flutter/material.dart';

class InstituteCertificates extends StatelessWidget {
  const InstituteCertificates({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
